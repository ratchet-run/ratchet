# Same PostgreSQL driver policy used by the Jakarta EE portability suite. Allows the server
# to infer native-query null parameter types and application String-to-JSONB bindings.
spring.datasource.hikari.data-source-properties.stringtype=unspecified
