<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>run.ratchet</groupId>
  <artifactId>ratchet-parent</artifactId>
  <version>0.1.2-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Ratchet</name>
  <description>Portable, CDI-based job scheduler for Jakarta EE 10+</description>
  <url>https://github.com/ratchet-run/ratchet</url>
  <inceptionYear>2026</inceptionYear>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>jcputney</id>
      <name>Jonathan Putney</name>
      <email>jputney@noverant.com</email>
      <url>https://github.com/jcputney</url>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/ratchet-run/ratchet.git</connection>
    <developerConnection>scm:git:git@github.com:ratchet-run/ratchet.git</developerConnection>
    <url>https://github.com/ratchet-run/ratchet</url>
    <tag>HEAD</tag>
  </scm>

  <issueManagement>
    <system>GitHub Issues</system>
    <url>https://github.com/ratchet-run/ratchet/issues</url>
  </issueManagement>

  <modules>
    <module>ratchet-api</module>
    <module>ratchet</module>
    <module>ratchet-encryption</module>
    <module>stores/ratchet-store-core</module>
    <module>stores/ratchet-store-mysql</module>
    <module>stores/ratchet-store-postgresql</module>
    <module>stores/ratchet-store-mongodb</module>
    <module>coordinators/ratchet-coordinator-common</module>
    <module>coordinators/ratchet-coordinator-postgresql</module>
    <module>coordinators/ratchet-coordinator-jms</module>
    <module>coordinators/ratchet-coordinator-infinispan</module>
    <module>coordinators/ratchet-coordinator-hazelcast</module>
    <module>observability/ratchet-micrometer</module>
    <module>observability/ratchet-otel</module>
    <module>ratchet-bom</module>
    <module>testing/ratchet-tck</module>
    <module>testing/ratchet-testsuite</module>
    <module>testing/ratchet-testsuite-jpms</module>
    <module>testing/ratchet-loadtest</module>
    <module>testing/ratchet-showcase</module>
    <module>testing/ratchet-coverage</module>
    <module>testing/ratchet-arch-tests</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <!-- Reproducible builds: two builds of the same tag produce identical jar hashes.
         maven-release-plugin refreshes this on each release:prepare. -->
    <project.build.outputTimestamp>2026-06-24T14:25:05Z</project.build.outputTimestamp>
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <maven.compiler.release>17</maven.compiler.release>

    <!--
      Jakarta EE API versions. Defaults target Jakarta EE 10. The
      jakarta-ee11-verify profile overrides every version below to its EE 11
      counterpart so a single `mvn -P jakarta-ee11-verify clean compile` run
      retargets the entire API surface. We parameterize every API version
      independently rather than importing jakarta.jakartaee-bom because the
      platform BOM declares each API with `<exclusion>*:*</exclusion>`, which
      strips transitive deps (e.g. cdi-api → jakarta.inject) that ratchet's
      modular layout depends on.
    -->
    <jakarta.ee.version>10.0.0</jakarta.ee.version>
    <jakarta.cdi-api.version>4.0.1</jakarta.cdi-api.version>
    <jakarta.annotation-api.version>2.1.1</jakarta.annotation-api.version>
    <jakarta.inject-api.version>2.0.1</jakarta.inject-api.version>
    <jakarta.interceptor-api.version>2.1.0</jakarta.interceptor-api.version>
    <jakarta.transaction-api.version>2.0.1</jakarta.transaction-api.version>
    <jakarta.persistence-api.version>3.1.0</jakarta.persistence-api.version>
    <jakarta.json-api.version>2.1.0</jakarta.json-api.version>
    <jakarta.json.bind-api.version>3.0.0</jakarta.json.bind-api.version>
    <jakarta.security.enterprise-api.version>3.0.0</jakarta.security.enterprise-api.version>
    <!--
      jakarta.servlet-api and jakarta.authentication-api are not used directly
      by ratchet code, but jakarta.security.enterprise-api 4.0.0 (EE 11)
      uses `requires transitive` for both in its module-info, propagating the
      module-path requirement to any module that `requires jakarta.security`.
      Declared as provided deps so the modular compile resolves under both
      EE 10 (where 3.0.0's non-transitive requires made these unnecessary)
      and EE 11.
    -->
    <jakarta.servlet-api.version>6.0.0</jakarta.servlet-api.version>
    <jakarta.authentication-api.version>3.0.0</jakarta.authentication-api.version>
    <junit.version>5.14.4</junit.version>
    <junit.platform.version>1.14.4</junit.platform.version>
    <asm.version>9.7.1</asm.version>
    <cron-utils.version>9.2.1</cron-utils.version>
    <arquillian.version>1.10.1.Final</arquillian.version>
    <!-- Plugin versions -->
    <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version>
    <maven-failsafe-plugin.version>3.5.5</maven-failsafe-plugin.version>
    <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
    <maven-war-plugin.version>3.5.1</maven-war-plugin.version>
    <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
    <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
    <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
    <maven-release-plugin.version>3.3.1</maven-release-plugin.version>
    <central-publishing-maven-plugin.version>0.10.0</central-publishing-maven-plugin.version>
    <build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
    <spotless-maven-plugin.version>2.46.1</spotless-maven-plugin.version>
    <google-java-format.version>1.35.0</google-java-format.version>
    <testcontainers.version>2.0.5</testcontainers.version>
    <micrometer.version>1.14.4</micrometer.version>
    <micrometer.tracing.version>1.4.4</micrometer.tracing.version>
    <opentelemetry.version>1.62.0</opentelemetry.version>
    <mockito.version>5.23.0</mockito.version>
    <awaitility.version>4.3.0</awaitility.version>
    <archunit.version>1.3.0</archunit.version>
    <shrinkwrap-resolver.version>3.3.6</shrinkwrap-resolver.version>
    <wildfly.version>39.0.1.Final</wildfly.version>
    <wildfly-arquillian.version>5.1.0.Final</wildfly-arquillian.version>
    <payara.version>6.2025.11</payara.version>
    <payara-arquillian.version>4.0.alpha4</payara-arquillian.version>
    <!--
      Eclipse GlassFish 8 is the Jakarta EE 11 reference implementation
      (8.0.0 GA Feb 5 2026; 8.0.1 latest stable). Requires JDK 21+ at
      runtime — the glassfish-managed testsuite profile enforces this with
      maven-enforcer-plugin. Default CI lanes still target JDK 17.
    -->
    <glassfish.version>8.0.1</glassfish.version>
    <glassfish-arquillian.version>2.1.3</glassfish-arquillian.version>
    <openliberty.version>26.0.0.2</openliberty.version>
    <openliberty-arquillian.version>2.1.4</openliberty-arquillian.version>
    <mysql-connector.version>8.4.0</mysql-connector.version>
    <protobuf-java.version>3.25.9</protobuf-java.version>
    <postgresql-driver.version>42.7.11</postgresql-driver.version>
    <jakarta.jms-api.version>3.1.0</jakarta.jms-api.version>
    <artemis.version>2.54.0</artemis.version>
    <netty.version>4.1.133.Final</netty.version>
    <commons-configuration2.version>2.15.0</commons-configuration2.version>
    <jackson-core.version>2.18.6</jackson-core.version>
    <infinispan.version>15.1.7.Final</infinispan.version>
    <hazelcast.version>5.5.0</hazelcast.version>
    <mongodb-driver.version>5.7.0</mongodb-driver.version>
    <hibernate.version>6.6.50.Final</hibernate.version>
    <jboss-logging.version>3.6.3.Final</jboss-logging.version>
    <maven-dependency-plugin.version>3.10.0</maven-dependency-plugin.version>
    <jacoco-maven-plugin.version>0.8.14</jacoco-maven-plugin.version>
    <flatten-maven-plugin.version>1.7.3</flatten-maven-plugin.version>
    <cyclonedx-maven-plugin.version>2.9.1</cyclonedx-maven-plugin.version>
    <license-maven-plugin.version>2.7.1</license-maven-plugin.version>
    <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>
    <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
    <spotbugs-maven-plugin.version>4.9.3.0</spotbugs-maven-plugin.version>
    <asm.version>9.8</asm.version>
    <pitest-maven-plugin.version>1.19.1</pitest-maven-plugin.version>
    <pitest-junit5-plugin.version>1.2.2</pitest-junit5-plugin.version>
    <argLine></argLine>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>jakarta.platform</groupId>
        <artifactId>jakarta.jakartaee-web-api</artifactId>
        <version>${jakarta.ee.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>jakarta.enterprise</groupId>
        <artifactId>jakarta.enterprise.cdi-api</artifactId>
        <version>${jakarta.cdi-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.annotation</groupId>
        <artifactId>jakarta.annotation-api</artifactId>
        <version>${jakarta.annotation-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.inject</groupId>
        <artifactId>jakarta.inject-api</artifactId>
        <version>${jakarta.inject-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.interceptor</groupId>
        <artifactId>jakarta.interceptor-api</artifactId>
        <version>${jakarta.interceptor-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.transaction</groupId>
        <artifactId>jakarta.transaction-api</artifactId>
        <version>${jakarta.transaction-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.persistence</groupId>
        <artifactId>jakarta.persistence-api</artifactId>
        <version>${jakarta.persistence-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.json</groupId>
        <artifactId>jakarta.json-api</artifactId>
        <version>${jakarta.json-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.json.bind</groupId>
        <artifactId>jakarta.json.bind-api</artifactId>
        <version>${jakarta.json.bind-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.jms</groupId>
        <artifactId>jakarta.jms-api</artifactId>
        <version>${jakarta.jms-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.artemis</groupId>
        <artifactId>artemis-jakarta-server</artifactId>
        <version>${artemis.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.artemis</groupId>
        <artifactId>artemis-jakarta-client</artifactId>
        <version>${artemis.version}</version>
      </dependency>
      <dependency>
        <groupId>org.infinispan</groupId>
        <artifactId>infinispan-core</artifactId>
        <version>${infinispan.version}</version>
      </dependency>
      <dependency>
        <groupId>com.hazelcast</groupId>
        <artifactId>hazelcast</artifactId>
        <version>${hazelcast.version}</version>
      </dependency>

      <!--
        Security pins for vulnerable dependencies pulled in transitively by the
        coordinator modules. These coordinates are not declared directly by any
        module, so they are overridden here to keep the resolved graph patched:
          * io.netty:*               (test scope, via the embedded Artemis broker)
          * commons-configuration2   (test scope, via the embedded Artemis broker)
          * jackson-core             (compile scope, via infinispan-core)
      -->
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-bom</artifactId>
        <version>${netty.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.commons</groupId>
        <artifactId>commons-configuration2</artifactId>
        <version>${commons-configuration2.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>${jackson-core.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.security.enterprise</groupId>
        <artifactId>jakarta.security.enterprise-api</artifactId>
        <version>${jakarta.security.enterprise-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.servlet</groupId>
        <artifactId>jakarta.servlet-api</artifactId>
        <version>${jakarta.servlet-api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>jakarta.authentication</groupId>
        <artifactId>jakarta.authentication-api</artifactId>
        <version>${jakarta.authentication-api.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>org.ow2.asm</groupId>
        <artifactId>asm</artifactId>
        <version>${asm.version}</version>
      </dependency>
      <dependency>
        <groupId>org.ow2.asm</groupId>
        <artifactId>asm-tree</artifactId>
        <version>${asm.version}</version>
      </dependency>

      <!-- Cron Utils -->
      <dependency>
        <groupId>com.cronutils</groupId>
        <artifactId>cron-utils</artifactId>
        <version>${cron-utils.version}</version>
      </dependency>

      <!-- Testing -->
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.junit.platform</groupId>
        <artifactId>junit-platform-launcher</artifactId>
        <version>${junit.platform.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.jboss.arquillian</groupId>
        <artifactId>arquillian-bom</artifactId>
        <version>${arquillian.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-junit-jupiter</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- Inter-module dependencies -->
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-store-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-encryption</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-store-mysql</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-store-postgresql</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-store-mongodb</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-coordinator-common</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-coordinator-postgresql</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-coordinator-jms</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-coordinator-infinispan</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-coordinator-hazelcast</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-tck-util</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-tck-store</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-tck-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-tck-jakarta</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-tck-coordinator</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-micrometer</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-otel</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-testsuite</artifactId>
        <version>${project.version}</version>
        <type>war</type>
      </dependency>
      <dependency>
        <groupId>run.ratchet</groupId>
        <artifactId>ratchet-arch-tests</artifactId>
        <version>${project.version}</version>
      </dependency>
      <!-- Testcontainers BOM -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Awaitility -->
      <dependency>
        <groupId>org.awaitility</groupId>
        <artifactId>awaitility</artifactId>
        <version>${awaitility.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- ArchUnit -->
      <dependency>
        <groupId>com.tngtech.archunit</groupId>
        <artifactId>archunit-junit5</artifactId>
        <version>${archunit.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- ShrinkWrap Resolver -->
      <dependency>
        <groupId>org.jboss.shrinkwrap.resolver</groupId>
        <artifactId>shrinkwrap-resolver-depchain</artifactId>
        <version>${shrinkwrap-resolver.version}</version>
        <type>pom</type>
        <scope>test</scope>
      </dependency>

      <!-- WildFly Arquillian -->
      <dependency>
        <groupId>org.wildfly.arquillian</groupId>
        <artifactId>wildfly-arquillian-container-managed</artifactId>
        <version>${wildfly-arquillian.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- Payara Arquillian -->
      <dependency>
        <groupId>fish.payara.arquillian</groupId>
        <artifactId>arquillian-payara-server-managed</artifactId>
        <version>${payara-arquillian.version}</version>
        <scope>test</scope>
      </dependency>

      <!--
        GlassFish 8 Arquillian connector. The community-maintained line lives
        under ee.omnifish.arquillian (the older org.omnifaces.arquillian
        groupId stopped at 1.8 and predates GlassFish 8 GA).
      -->
      <dependency>
        <groupId>ee.omnifish.arquillian</groupId>
        <artifactId>arquillian-glassfish-server-managed</artifactId>
        <version>${glassfish-arquillian.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- Open Liberty Arquillian -->
      <dependency>
        <groupId>io.openliberty.arquillian</groupId>
        <artifactId>arquillian-liberty-managed-jakarta-junit5</artifactId>
        <version>${openliberty-arquillian.version}</version>
        <type>pom</type>
        <scope>test</scope>
      </dependency>

      <!-- MySQL Connector -->
      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql-connector.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf-java.version}</version>
      </dependency>

      <!-- PostgreSQL Driver -->
      <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>${postgresql-driver.version}</version>
        <scope>test</scope>
      </dependency>

      <!-- MongoDB Driver -->
      <dependency>
        <groupId>org.mongodb</groupId>
        <artifactId>mongodb-driver-sync</artifactId>
        <version>${mongodb-driver.version}</version>
      </dependency>

      <!-- jboss-logging -->
      <dependency>
        <groupId>org.jboss.logging</groupId>
        <artifactId>jboss-logging</artifactId>
        <version>${jboss-logging.version}</version>
      </dependency>

      <!-- Hibernate (test-scope only; production JPA provider is chosen by the adopter) -->
      <dependency>
        <groupId>org.hibernate.orm</groupId>
        <artifactId>hibernate-core</artifactId>
        <version>${hibernate.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <profiles>
    <!--
      PIT mutation-testing profile. Opt-in and never bound to a default
      lifecycle phase: mutation analysis reruns the test suite once per
      mutant, so it is far too slow for `mvn verify`. It is a diagnostic you
      invoke explicitly to find weakly-asserted code that line coverage hides.

      Scoped by default to the RI core (run.ratchet.ri.core.*), the
      correctness-critical claim/lease/retry engine where mutation signal is
      most valuable. No mutationThreshold is set, so it never fails a build —
      it produces a surviving-mutant report you read.

      Usage (single module, recommended):
        mvn -P pit -pl ratchet -am test-compile org.pitest:pitest-maven:mutationCoverage
      Report lands at ratchet/target/pit-reports/ (HTML + XML).
    -->
    <profile>
      <id>pit</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>${pitest-maven-plugin.version}</version>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>${pitest-junit5-plugin.version}</version>
              </dependency>
            </dependencies>
            <configuration>
              <targetClasses>
                <param>run.ratchet.ri.core.*</param>
              </targetClasses>
              <targetTests>
                <param>run.ratchet.*</param>
              </targetTests>
              <!--
                Modules outside the target package (every module but ratchet)
                contribute no matching classes; without this they would fail
                the goal with "no mutations found".
              -->
              <failWhenNoMutations>false</failWhenNoMutations>
              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>
              <timestampedReports>false</timestampedReports>
              <!--
                Timing-sensitive tests (sleep/await-based) can hang or time
                out under a mutant that perturbs scheduling, producing noise
                rather than signal. Add such tests here after the first run
                if they prove flaky under mutation, e.g.:
                  <param>run.ratchet.ri.resilience.CircuitBreakerTest</param>
              -->
              <excludedTestClasses/>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
      Jakarta EE 11 API verification profile. Retargets every individual
      Jakarta API version property to its EE 11 release (CDI 4.1,
      persistence 3.2, security 4.0, annotation 3.0, interceptor 2.2, etc.)
      and the umbrella jakartaee-web-api to 11.0.0. Intended for compile-time
      verification only — runtime container version bumps (Payara 7,
      WildFly Preview, Open Liberty webProfile-11.0) are tracked separately.
      The default build remains EE 10 to preserve the existing managed-server
      test matrix.

      Usage: mvn -P jakarta-ee11-verify clean compile
    -->
    <profile>
      <id>jakarta-ee11-verify</id>
      <properties>
        <jakarta.ee.version>11.0.0</jakarta.ee.version>
        <jakarta.cdi-api.version>4.1.0</jakarta.cdi-api.version>
        <jakarta.annotation-api.version>3.0.0</jakarta.annotation-api.version>
        <!-- jakarta.inject-api stays at 2.0.1 in EE 11 (no spec bump). -->
        <jakarta.inject-api.version>2.0.1</jakarta.inject-api.version>
        <jakarta.interceptor-api.version>2.2.0</jakarta.interceptor-api.version>
        <!-- jakarta.transaction-api stays at 2.0.1 in EE 11. -->
        <jakarta.transaction-api.version>2.0.1</jakarta.transaction-api.version>
        <jakarta.persistence-api.version>3.2.0</jakarta.persistence-api.version>
        <jakarta.json-api.version>2.1.3</jakarta.json-api.version>
        <jakarta.json.bind-api.version>3.0.1</jakarta.json.bind-api.version>
        <jakarta.security.enterprise-api.version>4.0.0</jakarta.security.enterprise-api.version>
        <jakarta.servlet-api.version>6.1.0</jakarta.servlet-api.version>
        <jakarta.authentication-api.version>3.1.0</jakarta.authentication-api.version>
      </properties>
      <build>
        <plugins>
          <!--
            Jakarta EE 11 targets JDK 21+ at runtime. Fail fast at validate
            time so a stray JDK 17 invocation surfaces here, not three
            modules deep into the build with a confusing API/javac mismatch.
          -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
              <execution>
                <id>enforce-jdk21-for-ee11</id>
                <phase>validate</phase>
                <goals>
                  <goal>enforce</goal>
                </goals>
                <configuration>
                  <rules>
                    <requireJavaVersion>
                      <version>[21,)</version>
                      <message>
                        Jakarta EE 11 verification requires JDK 21 or higher.
                        Re-run with JDK 21+ on PATH (or via JAVA_HOME).
                      </message>
                    </requireJavaVersion>
                  </rules>
                  <fail>true</fail>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!--
      WildFly 40 / Jakarta EE 11 managed-server test profile. Enforces JDK 25 at
      runtime so the WF40 integration tests run on the recommended LTS. The server
      config and the EE 11-only test sources live in ratchet-testsuite's same-id
      profile. The ratchet library itself is unaffected: it stays a single
      EE-agnostic artifact built at the default JDK 17 baseline.
    -->
    <profile>
      <id>wildfly-ee11-managed</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
              <execution>
                <id>enforce-jdk25-for-wildfly-ee11</id>
                <phase>validate</phase>
                <goals>
                  <goal>enforce</goal>
                </goals>
                <configuration>
                  <rules>
                    <requireJavaVersion>
                      <version>[25,)</version>
                      <message>
                        The wildfly-ee11-managed profile targets WildFly 40 on JDK 25.
                        Re-run with JDK 25+ on PATH (or via JAVA_HOME).
                      </message>
                    </requireJavaVersion>
                  </rules>
                  <fail>true</fail>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>javadoc-aggregate</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <inherited>false</inherited>
            <executions>
              <execution>
                <id>aggregate-javadoc</id>
                <phase>package</phase>
                <goals>
                  <goal>aggregate</goal>
                </goals>
                <configuration>
                  <windowtitle>Ratchet ${project.version} API</windowtitle>
                  <doctitle>Ratchet ${project.version} - Portable CDI-based Job Scheduler</doctitle>
                  <header>Ratchet ${project.version}</header>
                  <footer>Ratchet ${project.version}</footer>
                  <overview>${basedir}/src/main/javadoc/overview.html</overview>
                  <sourcepath>
                    ${basedir}/ratchet-api/src/main/java:${basedir}/ratchet/src/main/java:${basedir}/stores/ratchet-store-core/src/main/java:${basedir}/ratchet-bom/src/main/java
                  </sourcepath>
                  <excludePackageNames>*.internal:*.impl:*.util</excludePackageNames>
                  <sourceFileExcludes>
                    <sourceFileExclude>**/module-info.java</sourceFileExclude>
                  </sourceFileExcludes>
                  <additionalJOption>--ignore-source-errors</additionalJOption>
                  <links>
                    <link>https://docs.oracle.com/en/java/javase/17/docs/api</link>
                    <link>https://jakarta.ee/specifications/platform/10/apidocs</link>
                  </links>
                  <bottom>Copyright &#169; {inceptionYear}-{currentYear} Ratchet Contributors. All rights reserved.</bottom>
                  <doclint>none</doclint>
                  <failOnError>false</failOnError>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>jacoco-offline</id>
      <dependencies>
        <dependency>
          <groupId>org.jacoco</groupId>
          <artifactId>org.jacoco.agent</artifactId>
          <classifier>runtime</classifier>
          <version>${jacoco-maven-plugin.version}</version>
          <scope>test</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>prepare-agent</id>
                <phase>none</phase>
              </execution>
              <execution>
                <id>offline-instrument</id>
                <phase>process-classes</phase>
                <goals>
                  <goal>instrument</goal>
                </goals>
              </execution>
              <!-- restore before jacoco report at verify -->
              <execution>
                <id>offline-restore</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>restore-instrumented-classes</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <argLine>
                -XX:+EnableDynamicAgentLoading ${argLine}
                -Djacoco-agent.destfile=${project.build.directory}/jacoco.exec
                -Djacoco-agent.append=true
              </argLine>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!-- Release publishing profiles. maven-release-plugin activates central+sign together
         via releaseProfiles during release:perform; none are active in normal builds. -->
    <profile>
      <id>sign</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>${maven-gpg-plugin.version}</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>central</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>${central-publishing-maven-plugin.version}</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>false</autoPublish>
              <waitUntil>validated</waitUntil>
              <!-- Literal artifact id, not ${project.artifactId}: the aggregated upload runs
                   in the last reactor module's deploy phase, so ${project.artifactId} would
                   label the whole bundle after whichever module sorts last (e.g.
                   ratchet-arch-tests) even though that module's jar is excluded. -->
              <deploymentName>run.ratchet:ratchet:${project.version}</deploymentName>
              <!-- Test/dev infrastructure stays out of the Central bundle. Central artifacts
                   are immutable, so a stray deploy of these could never be undone. Each of
                   these modules also sets maven.deploy.skip=true, which guards the plain
                   mvn-deploy path (e.g. GitHub Packages snapshots). -->
              <excludeArtifacts>
                <excludeArtifact>ratchet-testsuite</excludeArtifact>
                <excludeArtifact>ratchet-testsuite-jpms</excludeArtifact>
                <excludeArtifact>ratchet-loadtest</excludeArtifact>
                <excludeArtifact>ratchet-showcase</excludeArtifact>
                <excludeArtifact>ratchet-coverage</excludeArtifact>
                <excludeArtifact>ratchet-arch-tests</excludeArtifact>
              </excludeArtifacts>
            </configuration>
          </plugin>
          <!-- Central requires -sources.jar and -javadoc.jar for every jar artifact.
               Listed explicitly (instead of relying on the super-POM release-profile,
               which Maven 4 removes) so the managed attach-sources/attach-javadocs
               executions run whenever this profile is active. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>github</id>
      <distributionManagement>
        <repository>
          <id>github</id>
          <name>GitHub Packages</name>
          <url>https://maven.pkg.github.com/ratchet-run/ratchet</url>
        </repository>
      </distributionManagement>
    </profile>
  </profiles>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven-failsafe-plugin.version}</version>
          <configuration>
            <useModulePath>false</useModulePath>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven-jar-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <version>${maven-war-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>${maven-source-plugin.version}</version>
          <executions>
            <execution>
              <id>attach-sources</id>
              <goals>
                <goal>jar-no-fork</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven-javadoc-plugin.version}</version>
          <configuration>
            <additionalOptions>-Xdoclint:-missing</additionalOptions>
          </configuration>
          <executions>
            <execution>
              <id>attach-javadocs</id>
              <goals>
                <goal>jar</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-release-plugin</artifactId>
          <version>${maven-release-plugin.version}</version>
          <configuration>
            <!-- The release workflow gates on the full CI matrix before release:prepare
                 runs, so the forked builds skip tests and SpotBugs instead of repeating
                 the Testcontainers suites. -->
            <arguments>-ntp -DskipTests -Dspotbugs.skip=true</arguments>
            <scmCommentPrefix />
            <tagNameFormat>v@{project.version}</tagNameFormat>
            <releaseProfiles>central,sign</releaseProfiles>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>${maven-dependency-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless-maven-plugin.version}</version>
          <configuration>
            <java>
              <!--
                module-info.java is excluded so it falls through to the generic
                <formats> license step below; the java step's licenseHeader
                special-cases (skips) module descriptors, which would leave them
                unheadered and ungated.
              -->
              <excludes>
                <exclude>**/module-info.java</exclude>
              </excludes>
              <googleJavaFormat>
                <version>${google-java-format.version}</version>
                <style>GOOGLE</style>
              </googleJavaFormat>
              <removeUnusedImports/>
              <formatAnnotations/>
              <licenseHeader>
                <delimiter>(package|/\*\*|module)</delimiter>
                <content>/*
 * Copyright 2026 Ratchet Contributors
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
</content>
              </licenseHeader>
            </java>
            <!--
              The java step above only targets src/main/java and src/test/java,
              so it never sees the profile-only test roots (java-wildfly /
              java-ee11). This generic format stamps and gates the license header
              on those roots. module-info.java is handled separately by the
              maven-antrun header check below, because spotless's LicenseHeaderStep
              special-cases (skips) module descriptors entirely.
            -->
            <formats>
              <format>
                <includes>
                  <include>src/test/java-wildfly/**/*.java</include>
                  <include>src/test/java-ee11/**/*.java</include>
                </includes>
                <licenseHeader>
                  <delimiter>(package|module|open|import|/\*\*)</delimiter>
                  <content>/*
 * Copyright 2026 Ratchet Contributors
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
</content>
                </licenseHeader>
              </format>
            </formats>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-antrun-plugin</artifactId>
          <version>${maven-antrun-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.cyclonedx</groupId>
          <artifactId>cyclonedx-maven-plugin</artifactId>
          <version>${cyclonedx-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>flatten-maven-plugin</artifactId>
          <version>${flatten-maven-plugin.version}</version>
          <configuration>
            <flattenMode>bom</flattenMode>
          </configuration>
          <executions>
            <execution>
              <id>flatten</id>
              <phase>process-resources</phase>
              <goals>
                <goal>flatten</goal>
              </goals>
            </execution>
            <execution>
              <id>flatten.clean</id>
              <phase>clean</phase>
              <goals>
                <goal>clean</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <!--
          License header management. Declared in pluginManagement only (no
          <executions> binding) so it is available for ad-hoc use via
          `mvn license:format` / `mvn license:check` without stamping every
          source file as part of the normal build.
        -->
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>license-maven-plugin</artifactId>
          <version>${license-maven-plugin.version}</version>
          <configuration>
            <licenseName>apache_v2</licenseName>
            <projectName>Ratchet</projectName>
            <organizationName>Ratchet Contributors</organizationName>
            <inceptionYear>${project.inceptionYear}</inceptionYear>
            <canUpdateCopyright>true</canUpdateCopyright>
            <canUpdateDescription>true</canUpdateDescription>
            <roots>
              <root>src/main/java</root>
              <root>src/test/java</root>
            </roots>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>${maven-enforcer-plugin.version}</version>
        </plugin>
        <!--
          SpotBugs static-analysis plugin, adopted for its multithreaded-
          correctness detectors (the MT_CORRECTNESS family) on this scheduler's
          concurrency-heavy code — a class of bug CodeQL and Qodana underweight.

          Effort is Max for the deepest dataflow analysis. The include filter
          (spotbugs-include.xml) narrows reporting to the high-signal categories
          (concurrency + definite correctness) so the gate stays low-noise. That
          scoping IS the baseline — there is no instance-suppression file because
          the findings present at adoption were fixed, not suppressed, so the gate
          runs clean and any NEW concurrency/correctness finding fails the build.
        -->
        <plugin>
          <groupId>com.github.spotbugs</groupId>
          <artifactId>spotbugs-maven-plugin</artifactId>
          <version>${spotbugs-maven-plugin.version}</version>
          <!--
            SpotBugs 4.9.3 pins ASM 9.7.1, whose ClassReader rejects Java 25
            bytecode (class file major version 69). The analyzer parses the
            *running* JDK's runtime classes, so on a JDK 25 toolchain it fails
            with "Unsupported class file major version 69" before analyzing a
            single project class. Override the full ASM family to a build that
            understands Java 25. ASM is unshaded here, so this override takes.
          -->
          <dependencies>
            <dependency>
              <groupId>org.ow2.asm</groupId>
              <artifactId>asm</artifactId>
              <version>${asm.version}</version>
            </dependency>
            <dependency>
              <groupId>org.ow2.asm</groupId>
              <artifactId>asm-analysis</artifactId>
              <version>${asm.version}</version>
            </dependency>
            <dependency>
              <groupId>org.ow2.asm</groupId>
              <artifactId>asm-commons</artifactId>
              <version>${asm.version}</version>
            </dependency>
            <dependency>
              <groupId>org.ow2.asm</groupId>
              <artifactId>asm-tree</artifactId>
              <version>${asm.version}</version>
            </dependency>
            <dependency>
              <groupId>org.ow2.asm</groupId>
              <artifactId>asm-util</artifactId>
              <version>${asm.version}</version>
            </dependency>
          </dependencies>
          <configuration>
            <effort>Max</effort>
            <threshold>Low</threshold>
            <includeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-include.xml</includeFilterFile>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!--
        Baseline environment guardrails. Enforces the Java 17 minimum the
        compiler is already configured for and a Maven 3.8.x floor (Maven
        3.8.1+ is required for many modern plugins, including spotbugs 4.9.x).
        Bound to validate so misconfigured local environments fail fast,
        before the reactor pulls in module sources.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <configuration>
          <rules>
            <requireJavaVersion>
              <version>[17,)</version>
              <message>Ratchet requires JDK 17 or higher to build.</message>
            </requireJavaVersion>
            <requireMavenVersion>
              <version>[3.8.1,)</version>
              <message>Ratchet requires Maven 3.8.1 or higher.</message>
            </requireMavenVersion>
          </rules>
          <fail>true</fail>
        </configuration>
        <executions>
          <execution>
            <id>enforce-baseline</id>
            <phase>validate</phase>
            <goals>
              <goal>enforce</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-XX:+EnableDynamicAgentLoading ${argLine}</argLine>
          <useModulePath>false</useModulePath>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <!--
            spotless's LicenseHeaderStep silently skips module-info.java, so the
            spotless:check gate above cannot enforce headers on module descriptors.
            This check fails the build if any module-info.java lacks the Apache-2.0
            header, closing that gap so descriptors cannot regress.
          -->
          <execution>
            <id>check-module-info-license-header</id>
            <phase>verify</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <fileset id="unheadered.module.info" dir="${project.basedir}/src/main/java"
                         includes="module-info.java" erroronmissingdir="false">
                  <not>
                    <contains text="Licensed under the Apache License, Version 2.0"/>
                  </not>
                </fileset>
                <pathconvert refid="unheadered.module.info"
                             property="unheadered.module.info.path" setonempty="false"/>
                <fail if="unheadered.module.info.path"
                      message="module-info.java is missing the Apache-2.0 license header: ${unheadered.module.info.path}"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!--
        SpotBugs concurrency/correctness gate, bound to verify so `mvn verify`
        fails on any new MT_CORRECTNESS or CORRECTNESS finding. Analysis config,
        the include filter, and the Java-25 ASM override are inherited from
        pluginManagement. CI runs this once in a dedicated job; the integration
        matrix passes -Dspotbugs.skip=true to avoid re-analyzing 15× per build.
      -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>spotbugs-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>build-sbom</id>
            <phase>package</phase>
            <goals>
              <goal>makeBom</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <projectType>library</projectType>
          <schemaVersion>1.6</schemaVersion>
          <outputFormat>all</outputFormat>
          <includeTestScope>false</includeTestScope>
          <!-- The central-publishing extension disables the standard deploy step for
               every module, which the default skipNotDeployed=true misreads as "don't
               need an SBOM" — release builds would silently ship without bom.{json,xml}. -->
          <skipNotDeployed>false</skipNotDeployed>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
